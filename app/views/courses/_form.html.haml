= form_for @course, :html => {:multipart => true } do |f|
  / - if @course.errors.any?
  /   #error_explanation
  /     %h2= "#{pluralize(@course.errors.count, "error")} prohibited this course from being saved:"
  /     %ul
  /       - @course.errors.full_messages.each do |msg|
  /         %li= msg
  / .span5
  / .login
  /   %section#content1
  /     = form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  /       %h1 Sign up
  /       .row
  /         .form-inline
  /           = f.label :Name, :id => "register-label"
  /           = f.text_field :first_name, :autofocus => true, :placeholder => "First Name"
  /           / = f.label :last_name
  /           = f.text_field :last_name, :placeholder => "Last Name"
  /       .form-inline
  /         = f.label :email, :id => "register-label"
  /         = f.email_field :email, :placeholder => "Your Email", :type => "text", :id => "username"
  /       .form-inline
  /         = f.label :password, :id => "register-label"
  /         = f.password_field :password, :placeholder => "New Password"
  /       #submit-button
  /         = f.submit "Sign up"
  .span
    .form-inline
      = f.label "Course title:", :id => "register-label"
      = f.text_field :name
  / .field
  /   = f.label :description
  /   = f.text_area :description
  / .field
  /   = f.label :price
  /   = f.text_field :price
  .field
    = f.label "Credits:", :id => "register-label"
    = f.number_field :credit
  / .field
  /   = f.label :hours
  /   = f.text_field :hours
  .form-inline
    = f.label "upload certificate:", :id => "file-upload"
    = f.file_field :avatar, :id => "file-upload"
  %h1 Course Attribute:
  - if @sub_req != nil then
    - @sub_req.each do |k|
      .form-inline{:id => "distro-button"}
        = f.label k.distro, :id => "register-label"
        = f.radio_button :distro, k.distro
  / .field
  /   = f.label :distro
  /   = f.check_box :distro
  / .field
  /   = f.hidden_field :user_id, :value => current_user.id
  .actions
    = f.submit 'Save'
  / = link_to 'Back', courses_path